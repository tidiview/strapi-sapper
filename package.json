{
  "private": true,
  "name": "sanity-sapper-blog",
  "version": "1.0.0",
  "scripts": {
    "build": "lerna run build --parallel",
    "build-studio": "lerna bootstrap && (cd strapi && yarn run strapi build)",
    "build-web": "lerna bootstrap && (cd sapper && yarn run export)",
    "dev": "lerna run dev --parallel",
    "format": "lerna run format",
    "lint": "lerna run lint",
    "postinstall": "lerna bootstrap",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "lerna": "^3.22.0"
  }
}